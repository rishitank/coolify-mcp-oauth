{
  "name": "coolify-mcp-oauth",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Self-hosted OAuth 2.1 authorization server (Google Sign-In) that fronts the @masonator/coolify-mcp server as a multi-tenant remote MCP endpoint.",
  "license": "MIT",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2",
    "jose": "^5.9.6",
    "oidc-provider": "^9.0.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^3.0.5"
  }
}
